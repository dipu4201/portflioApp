class Project {
  final String title;
  final String description;
  final String image; // Declare the image property

  Project({
    required this.title,
    required this.description,
    required this.image, // Initialize the image property
  });
}
